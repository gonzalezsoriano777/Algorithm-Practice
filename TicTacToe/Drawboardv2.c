#include <stdio.h>
#include <stdlib.h>


// This is the 2nd Version of the drawboard, only include the height and the width of the board
int drawboard(int, int);

int main(void)
{
    printf("Hello, World\n");


    drawboard(6, 4);
}

int drawboard(int x, int y)
{
    int size = 1;

    for(int i = 0; i < 0; i++)
    {

    }

    return size;
}
