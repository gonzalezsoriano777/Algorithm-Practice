#include  <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <cs50.h>


char nonRepeating(char *string);

int main(void)
{

  char newstring[] = "babah"

  for(i = 0; i < strlen(newstring[]); i++)
  {

      bool seen = false;

      for (j = 0; j < strlen(); j++)
      {
          if (strlen[i] == strlen[j])
          {
              if (seen)
              {
                  seen = false;
                  break;
              }
              else
              {
                  seen = true;
              }
          }
      }
  }



}

char nonRepeating(char *string)
{
    int counter = 0;

}




